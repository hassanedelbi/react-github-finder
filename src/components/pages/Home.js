import Alert from '../layout/Alert';
import Search from '../users/Search';
import UserList from '../users/UserList';
const Home = () => {
	return (
		<>
			<Alert />
			<Search />
			<UserList />
		</>
	);
};

export default Home;
